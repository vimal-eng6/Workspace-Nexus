{
  "name": "office-sync-hub",
  "version": "1.0.0",
  "description": "Office Sync: Room & IT Operations Hub",
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && python -m pip install -r backend/requirements.txt",
    "build:frontend": "npm run build --prefix frontend",
    "start:backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "start:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
